-nH
  -i ;--ignorecase      大文字・小文字無視
  -G ;--basic-regexp    基本正規表現
  -E ;--extended-regexp 拡張正規表現
  -P ;--perl-regexp     Perl正規表現
  -w ;--word-regexp     単語全体一致
  -x ;--line-regexp     行全体一致
  -v ;--invert-match    一致しない行を表示
  -s ;--no-messages     エラーメッセージ非表示
  -m ;[num]--max-count  [num]回ぶん処理する
  -b ;--byte-offset     行の何文字目か表示
  --include= ;[pat]     [pat]にマッチするファイル対象
  --exclude= ;[pat]     [pat]にマッチするファイル除外
  --exclude-dir= ;[pat] [pat]にマッチするディレクトリ除外
  -B ;[num]--before-context   一致行の前[num]行を表示
  -A ;[num]--after-context    一致行の後[num]行を表示
  -C ;[num]--context          一致行の前後[num]行を表示
  -r ;--recursive             サブディレクトリも対象
  -R ;--dereference-recursive サブディレクトリ、シンボリックリンクも対象
